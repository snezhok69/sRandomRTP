package org.sRandomRTP.Rtp;

/**
 * Default concrete RTP handler with no overrides.
 * Uses all {@link AbstractRtpHandler} defaults: random coordinate generation,
 * no biome filtering, standard radius from {@code teleport.yml}.
 */
public final class StandardRtpHandler extends AbstractRtpHandler {}
